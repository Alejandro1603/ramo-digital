function startAnimation() {
    // Aquí haremos la transición a la siguiente parte (ramo, mensaje, etc.)
    alert("¡Vamos a lo bonito! 🌻✨");
    // Luego aquí se puede cargar la siguiente sección
  }
  